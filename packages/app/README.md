# TrackCast Shared App

Shared React frontend for the macOS and Windows Tauri apps.

Edit this package when changing:

- live view UI
- setup/settings UI
- history/export UI
- app-level copy and visual polish

Platform packaging, sidecars, icons, and installer settings live in `apps/mac`
and `apps/windows`.
